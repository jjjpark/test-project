<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.demo.dao.RoomDao">
	<insert id="roomcreate">
		insert into room (title, position, memo, champion) values (#{title}, #{position}, #{memo}, #{champion})
	</insert>
	<select id="roomsearch">
		select * from room order by t_id desc limit 10;
	</select>
	
	<select id="roomData">
		select * from room order by t_id desc limit 1;
	</select>
</mapper>